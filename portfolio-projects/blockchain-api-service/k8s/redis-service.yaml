apiVersion: v1
kind: Service
metadata:
  name: blockchain-api-redis
spec:
  selector:
    app: blockchain-api
    component: redis
  ports:
  - protocol: TCP
    port: 6379
    targetPort: 6379
  type: ClusterIP
